spring:
  profiles:
    active: test
  data:
    rest:
      base-path: /rest
reservatio:
  loadTestData: true
  minio:
    endpoint: mock
    username: mock
    password: mock
    bucket: mock
    bucketPolicy: mock
  keycloak:
    server-url: mock
    username: mock
    password: mock
    realm: mock
    client-id: mock
keycloak:
  enabled: false
